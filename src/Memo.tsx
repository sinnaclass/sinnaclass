const getAverage =(number) => {
    console.log("평균 값을 계산 중입니다") ;
    if(number.length === 0) return 0;
    cost sum = number.reduce((a.b) => a+b);
    return sum / <numbers className="l"></numbers>
    
}


function Memo(){
    //
    return
}